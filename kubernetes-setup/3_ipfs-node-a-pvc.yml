apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ipfs-node-a-pvc
  namespace: overlay-network
spec:
  storageClassName: ipfs-node-a-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi