apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-node-b-pvc
  namespace: overlay-network
spec:
  storageClassName: data-node-b-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi