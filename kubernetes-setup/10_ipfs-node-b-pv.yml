apiVersion: v1
kind: PersistentVolume
metadata:
  name: ipfs-node-b-pv
spec:
  storageClassName: ipfs-node-b-storage
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/ipfs/node-b"